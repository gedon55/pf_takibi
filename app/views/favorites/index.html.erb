<div class='container-fluid'>
<div class='row'>
<div class='col'>
  
  <h2><i class="fas fa-campground"></i>気になるキャンプ場</h2>
  
  <div class='row'>
  <% @favorites.each do |favorite| %>
    <div class="col-md-2">
      <% if favorite.post.images.attached? %>
    　<!--↓.firstと付けることで複数の写真の中の一番最初の写真を表示-->
        <%= image_tag favorite.post.images.first, size: "200x200" %>
      <!--↓写真全部表示用の記述ループさせる-->
      <%# post.images.each do |image| %>
        <%#= image_tag image, size: "200x200" %>
      <%# end %>
      <% else %>
        <%= image_tag "leavelogo.jpg", size: "200x200" %>
      <% end %></br>
      
      <%= link_to favorite.post.place, post_path(favorite.post.id)%>
    </div>
  <% end %>
  </div>
  
</div>
</div>
</div>
