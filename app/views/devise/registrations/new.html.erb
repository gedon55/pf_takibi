<div class='container'>
<div class='row'>
<div class='mx-auto'>

  <h2>新規会員登録</h2>

<%= form_with model: @user, url: user_registration_path do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :姓 %><br/>
    <%= f.text_field :last_name, autofocus: true, autocompleate: "姓" %>
  </div>

  <div class="field">
    <%= f.label :名 %><br/>
    <%= f.text_field :first_name, autofocus: true, autocompleate: "名" %>
  </div>

  <div class="field">
    <%= f.label :ニックネーム（アプリ内で表示する名前です） %><br/>
    <%= f.text_field :nickname, autofocus: true, autocompleate: "ニックネーム" %>
  </div>

  <div class="field">
    <%= f.label :メールアドレス %><br/>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :パスワード %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> 文字以上)</em>
    <% end %><br/>
    <%= f.password_field :password, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :パスワード（確認用） %><br/>
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>
  
  <!--パスワードと新規登録ボタンの間を空けるための<br/>-->
  <br/>
  <div class="actions">
    <%= f.submit "新規登録" %>
  </div>
<% end %>

  <p>既に登録済みの方は<br/>
  <%= link_to "こちら", new_user_session_path, class: "underline" %>からログインを行なってください。</p>

</div>
</div>
</div>