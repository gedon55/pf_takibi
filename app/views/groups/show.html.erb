<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-8 offset-md-1'>
      <h2>Group Detail</h2>
      <table class='table table-hover table-inverse'>
        <thead>
          <tr>
            <th></th>
            <th>グループ名</th>
            <th>紹介文</th>
            <th>メンバー</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
        <tr>
          <td>
          <% if @group.image.attached? %>
            <%= image_tag @group.image, size: "200x200" %>
          <% else %>
            <%= image_tag 'no_image', size: "200x200" %>
          <% end %>
          </td>
          <td><%= @group.name %></td>
          <td><%= @group.introduction %></td>
          <td>
          <% @group.users.each do |member| %>
            <%= member.nickname %><br>
          <% end %>
          </td>
          <td>
            <% if @group.owner_id == current_user.id %>
            <%= link_to 'delete',group_all_destroy_path(@group),method: :delete,data: { confirm: '本当に削除しますか？' } ,class: "btn btn-sm btn-outline-danger" %>
            <% end %>
          </td>
        </tr>
      </table>
    </div>
  </div>
</div>